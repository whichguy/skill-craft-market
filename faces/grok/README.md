# Grok face

1. Skill-dir (recommended): clone skill-craft @ tag → `./install.sh --grok-only`
2. Optional: add skill-craft as git marketplace source later
3. Never pass Claude-style `name@marketplace` to `grok plugin install` (@ is git ref)
